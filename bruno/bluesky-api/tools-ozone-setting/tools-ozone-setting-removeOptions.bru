meta {
  name: tools-ozone-setting-removeOptions
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/xrpc/tools.ozone.setting.removeOptions
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "keys": [],
    "scope": ""
  }
}
