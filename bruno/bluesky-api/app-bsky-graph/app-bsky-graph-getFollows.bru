meta {
  name: app-bsky-graph-getFollows
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/xrpc/app.bsky.graph.getFollows
  body: none
  auth: bearer
}

params:query {
  actor: 
  ~limit: 
  ~cursor: 
}

auth:bearer {
  token: {{token}}
}
