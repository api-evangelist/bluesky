{
  "opencollection": "1.0.0",
  "info": {
    "name": "Bluesky Social Actor Profiles Video API",
    "version": "1.0.0"
  },
  "items": [
    {
      "info": {
        "name": "Video",
        "type": "folder"
      },
      "items": [
        {
          "info": {
            "name": "Bluesky Get status details for a video processing job.",
            "type": "http"
          },
          "http": {
            "method": "GET",
            "url": "https://bsky.social/xrpc/xrpc/app.bsky.video.getJobStatus",
            "params": [
              {
                "name": "jobId",
                "value": "example-jobId",
                "type": "query"
              }
            ],
            "auth": {
              "type": "bearer",
              "token": "{{bearerToken}}"
            }
          },
          "docs": "*This endpoint is part of the Bluesky application Lexicon APIs (`app.bsky.*`). Public endpoints which don't require authentication can be made directly against the public Bluesky AppView API: https://public.api.bsky.app. Authenticated requests are usually made to the user's PDS, with automatic service proxying. Authenticated requests can be used for both public and non-public endpoints.*\n\n*To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advance"
        },
        {
          "info": {
            "name": "Bluesky Get video upload limits for the authenticated user.",
            "type": "http"
          },
          "http": {
            "method": "GET",
            "url": "https://bsky.social/xrpc/xrpc/app.bsky.video.getUploadLimits",
            "auth": {
              "type": "bearer",
              "token": "{{bearerToken}}"
            }
          },
          "docs": "*This endpoint is part of the Bluesky application Lexicon APIs (`app.bsky.*`). Public endpoints which don't require authentication can be made directly against the public Bluesky AppView API: https://public.api.bsky.app. Authenticated requests are usually made to the user's PDS, with automatic service proxying. Authenticated requests can be used for both public and non-public endpoints.*\n\n*To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advance"
        },
        {
          "info": {
            "name": "Bluesky Upload a video to be processed then stored on the PDS.",
            "type": "http"
          },
          "http": {
            "method": "POST",
            "url": "https://bsky.social/xrpc/xrpc/app.bsky.video.uploadVideo",
            "auth": {
              "type": "bearer",
              "token": "{{bearerToken}}"
            }
          },
          "docs": "*This endpoint is part of the Bluesky application Lexicon APIs (`app.bsky.*`). Public endpoints which don't require authentication can be made directly against the public Bluesky AppView API: https://public.api.bsky.app. Authenticated requests are usually made to the user's PDS, with automatic service proxying. Authenticated requests can be used for both public and non-public endpoints.*\n\n*To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advance"
        }
      ]
    }
  ],
  "bundled": true
}