meta {
  name: app-bsky-graph-getMutes
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/xrpc/app.bsky.graph.getMutes
  body: none
  auth: bearer
}

params:query {
  ~limit: 
  ~cursor: 
}

auth:bearer {
  token: {{token}}
}
