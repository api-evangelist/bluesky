meta {
  name: app-bsky-notification-getUnreadCount
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/xrpc/app.bsky.notification.getUnreadCount
  body: none
  auth: bearer
}

params:query {
  ~priority: 
  ~seenAt: 
}

auth:bearer {
  token: {{token}}
}
