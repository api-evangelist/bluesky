meta {
  name: app-bsky-feed-getActorFeeds
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/xrpc/app.bsky.feed.getActorFeeds
  body: none
  auth: bearer
}

params:query {
  actor: 
  ~limit: 
  ~cursor: 
}

auth:bearer {
  token: {{token}}
}
